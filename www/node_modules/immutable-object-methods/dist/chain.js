"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

exports.default = function (unchainedMethods) {
  var map = unchainedMethods.map;


  var chain = function chain(input) {
    var methods = map(unchainedMethods, function (fn) {
      return function () {
        for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }

        return chain(fn.apply(undefined, [input].concat(args)));
      };
    });

    methods.value = input;

    return methods;
  };

  return function (input) {
    return chain(input);
  };
};