"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _asyncToGenerator2 = _interopRequireDefault(require("@babel/runtime/helpers/asyncToGenerator"));

const path = require(`path`);

const _require = require(`gatsby-source-filesystem/create-file-node`),
      createFileNode = _require.createFileNode;

let placeholderFileNode;

const getPlaceholderFileNode =
/*#__PURE__*/
function () {
  var _ref = (0, _asyncToGenerator2.default)(function* ({
    createNode,
    createNodeId
  }) {
    if (!placeholderFileNode) {
      const fileNode = yield createFileNode(path.join(__dirname, `placeholder.png`), createNodeId, {});
      createNode(fileNode, {
        name: `gatsby-source-filesystem`
      });
      placeholderFileNode = fileNode;
    }

    return placeholderFileNode;
  });

  return function getPlaceholderFileNode(_x) {
    return _ref.apply(this, arguments);
  };
}();

module.exports = getPlaceholderFileNode;